# ClipRelay 部署指南

三种部署方案，按需选择 — 从单机临时使用到公网生产级部署，灵活切换，兼容升级。

---

## 方案速览

| | 方案 A：本地 | 方案 B：Docker | 方案 C：Cloudflare Tunnel |
|---|---|---|---|
| **适合场景** | 个人单机、临时使用 | NAS、服务器常驻 | 公网服务、多用户 |
| **前置要求** | Node.js 18+ | Docker | Docker + CF 账号 + 域名 |
| **外网访问** | 仅局域网 | 需另配 Tunnel | ✅ 自带公网 CDN |
| **启动命令** | `npm start` | `docker compose up -d` | 多组件协同 |
| **资源占用** | ~50MB | ~60MB | ~60MB |
| **难度** | ⭐ | ⭐⭐ | ⭐⭐⭐ |

> 三种方案共享同一份配置文件和数据目录，可以无缝迁移。

---

## 目录

- [方案 A：本地部署](#方案-a本地部署)
- [方案 B：Docker 部署](#方案-bdocker-部署)
- [方案 C：Cloudflare Tunnel 公网部署](#方案-ccloudflare-tunnel-公网部署)
- [配置文件参考](#配置文件参考)
- [平台专项说明](#平台专项说明)
- [踩坑记录](#踩坑记录)
- [常见问题](#常见问题)

---

## 方案 A：本地部署

适合在个人电脑上快速启动，临时使用。

### 1. 安装依赖

```bash
cd clip-relay
npm install
```

### 2. 启动

```bash
npm start
```

终端输出：

```
[clip-relay] 已生成随机管理员密码 → 4inkzrd9j5s2
[clip-relay] 服务已启动 → http://0.0.0.0:3000
[clip-relay] 管理后台 → http://0.0.0.0:3000/admin
```

记下管理员密码，浏览器打开 `http://localhost:3000/admin` 登录。

### 3. 局域网共享

1. 查电脑局域网 IP：Windows 用 `ipconfig`，macOS/Linux 用 `ip addr`
2. 手机浏览器访问 `http://<电脑IP>:3000/?token=<房间令牌>`
3. 确保防火墙允许 3000 端口，手机和电脑在同一 Wi-Fi

### 4. 开发模式

```bash
npm run dev
```

文件变更自动重启。

---

## 方案 B：Docker 部署

适合 24 小时在线的设备（NAS、VPS、软路由等）。

### 1. 准备目录

```bash
mkdir -p /opt/clip-relay
cd /opt/clip-relay

# 复制项目文件到此目录，或 git clone
```

### 2. 创建 docker-compose.yml

```yaml
services:
  clip-relay:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - clip-relay-data:/app/data
    restart: unless-stopped
    environment:
      - PORT=3000
      - NODE_ENV=production

volumes:
  clip-relay-data:
```

> 如需与 Cloudflare Tunnel 配合（方案 C），容器需加入 Tunnel 所在的外部网络，`ports` 可省略。

### 3. 启动

```bash
docker compose up -d

# 查看日志，获取管理员密码
docker compose logs clip-relay | grep "管理员密码"
```

### 4. 构建问题排查

如果 Docker 构建时 npm install 失败，参考下表：

| 问题 | 原因 | 解决 |
|------|------|------|
| `EAI_AGAIN` registry 域名 | DNS 被拦截/污染 | 加 `network: host` 到 compose 的 build 配置 |
| 下载慢/超时 | 访问国外 registry 慢 | 在 Dockerfile 中添加 npm 镜像源 |
| `npm ERR!` 证书错误 | 镜像源证书问题 | 换用不同的镜像源 |

---

## 方案 C：Cloudflare Tunnel 公网部署

将内网服务通过 Cloudflare Tunnel 暴露到公网，无需公网 IP、无需端口转发、自动 HTTPS。

### 架构

```
用户 → relay.yourdomain.com → Cloudflare CDN → Tunnel → 内网 Docker 主机
                                                            │
                                                  cloudflared 容器
                                                            │
                                                  clip-relay 容器:3000
```

### 前置条件

- Cloudflare 账号（免费注册，无需信用卡）
- 一个托管在 Cloudflare DNS 的域名
- 一台内网 Docker 主机

### Step 1: 在 Cloudflare 创建 Tunnel

```bash
# 安装 cloudflared（https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/）
cloudflared tunnel login
cloudflared tunnel create clip-relay

# 会生成一个隧道 ID，如 455af5a5-2bf2-4e7c-a08e-b25915de921a
```

### Step 2: 配置 Tunnel

```yaml
# config.yml
tunnel: <你的隧道ID>
credentials-file: /path/to/<隧道ID>.json

ingress:
  - hostname: relay.yourdomain.com
    service: http://clip-relay:3000    # Docker 网络内通过容器名访问
  - service: http_status:404
```

### Step 3: 部署 ClipRelay

clip-relay 需要与 cloudflared 在同一个 Docker 网络才能通过容器名通信：

```yaml
services:
  clip-relay:
    build: .
    networks:
      - cloudflared-network    # 改为你的 cloudflared 所在网络
    volumes:
      - clip-relay-data:/app/data
    restart: unless-stopped
    environment:
      - PORT=3000
      - NODE_ENV=production

networks:
  cloudflared-network:
    external: true
```

> 查看 cloudflared 所在网络：`docker inspect cloudflared | grep -A5 Networks`

### Step 4: 配置 DNS 并启动

```bash
# 添加 DNS 记录
docker exec cloudflared cloudflared tunnel route dns \
  <隧道ID> relay.yourdomain.com

# 启动 ClipRelay
docker compose up -d

# 重启 cloudflared 加载新配置
docker restart cloudflared

# 查看管理员密码
docker compose logs | grep "管理员密码"
```

---

## 配置文件参考

```json
{
  "defaultToken": "clip-relay",
  "port": 3000,
  "maxImageSize": 5242880,
  "domain": "",
  "adminPassword": ""
}
```

| 字段 | 说明 |
|------|------|
| `defaultToken` | 默认房间令牌 |
| `port` | 服务端口 |
| `maxImageSize` | 单张图片上限（字节），默认 5MB |
| `domain` | 公网域名，配置后复制链接出现公网选项 |
| `adminPassword` | 管理员密码，留空则首次启动随机生成 |

---

## 平台专项说明

### N1 / OpenWRT 专项

N1 盒子刷 istoreOS / OpenWRT 后的 Docker 部署有一些特殊之处。

**SSH 密钥路径**：istoreOS 使用 dropbear，公钥必须放在：

```bash
/etc/dropbear/authorized_keys
# 不是 ~/.ssh/authorized_keys
```

**Docker 构建 DNS 拦截**：如果安装了 PassWall/SSR 等工具，它们拦截 DNS 导致 npm registry 域名返回 fake IP（198.18.0.x 段）：

```yaml
# docker-compose.yml 中加 network: host 绕过
build:
  context: .
  network: host
```

构建时也需要指定：

```bash
docker compose build --network host
```

**存储路径**：N1 eMMC 空间有限，建议数据目录放外置存储：

```yaml
volumes:
  - /mnt/sda1/clip-relay-data:/app/data
```

### 群晖 NAS 专项

```yaml
# Docker 套件中，文件路径使用绝对路径
services:
  clip-relay:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - /volume1/docker/clip-relay/data:/app/data
    restart: unless-stopped
```

群晖 Docker 套件中「文件管理器」路径需从 File Station 查看。

---

## 踩坑记录

### 1. 含中文 Token 的房间删除失败

**原因**：浏览器自动对 URL 中的中文进行百分号编码，服务端用 `split('/')` 提取 token 时拿到编码后的字符串，传给 SQLite 查不到原始 token。

**修复**：`server.js` 中路由处理加 `decodeURIComponent()`：

```js
const token = decodeURIComponent(parts[4]);
```

### 2. Docker 容器重启后密码变更

**原因**：`config.json` 中 `adminPassword` 为空，每次启动都生成新密码覆盖数据库。

**修复**：启动时判断数据库已有非默认密码则跳过生成（已在 `server.js` 中修复）。

### 3. 过期房间只清消息不删记录

**原因**：定时清理循环只调了 `deleteRoomMessages`，没调 `deleteRoom`。

**修复**：

```js
for (const token of expired) {
  deleteRoom(token);  // 同时删除消息和房间记录
}
```

### 4. Docker 构建缓存不一致

**原因**：`docker build --network host` 单独构建成功，但 `docker compose up -d` 使用自己内部的缓存用默认网络模式重新构建。

**修复**：在 `docker-compose.yml` 中显式声明 `build.network: host`。

---

## 常见问题

### Q: 三种方案选哪个？

- 只是自己用、电脑不常开 → **方案 A**
- 有 NAS/N1、24 小时在线 → **方案 B**
- 想给朋友用、追求稳定 → **方案 B + Cloudflare Tunnel**

### Q: 手机访问不到电脑？

1. 确认同一 Wi-Fi
2. 检查防火墙放行 3000 端口
3. 路由器 AP 隔离需关闭

### Q: 忘记管理员密码？

查看 `config.json` 的 `adminPassword` 字段，或删除该字段重启服务重新生成。

### Q: 数据备份？

复制 `data/` 目录即可。Docker 方案直接备份 volume 目录。

### Q: 容器重启密码变了？

确保使用最新版 `server.js`，首次启动后数据库中的密码不会被 config.json 的空密码覆盖。

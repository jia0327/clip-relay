{
  "name": "clip-relay-cloudflare",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node embed-html.mjs && wrangler dev",
    "deploy": "node embed-html.mjs && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}